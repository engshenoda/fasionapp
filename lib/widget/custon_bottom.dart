import 'package:flutter/material.dart';

class CustonBottom extends StatelessWidget {
  const CustonBottom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}